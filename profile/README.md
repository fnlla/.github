# Fnlla

Fnlla builds a production-focused PHP framework ecosystem with:

- `fnlla/fnlla` starter application
- `fnlla/packages` public package distribution hub
- `fnlla/framework` source-of-truth framework repository

Security reports: `security@fnlla.co.uk`
